# WORD GRIMOIRE ― 作業のきまり

## 作業の進め方（重要）
0. **ユーザーへの返事は日本語で書く**（途中で英語に切り替わったことがある。コードやプロンプトの英語はそのままでよい）
1. 作業ブランチで作業し、コミットして push する
2. **毎回、PR を作るところまで行う**（ユーザーの指示。「依頼がなければPRを作らない」の既定は、このリポジトリでは適用しない）
3. **マージはユーザーが行う**。こちらからマージはしない
4. マージ済みの PR は再利用できない。マージ後の追加作業は、ブランチを最新の `main` から作り直して **新しい PR** にする
5. **ブランチを `main` から作り直す前に、前の PR が本当にマージ済みか確かめる**（`git log origin/main` にその PR のマージが入っているか）。まだ開いている PR のブランチを作り直すと、その PR のコミットが消える

## 仕様書
- **`SPEC.md` が唯一の基準仕様**。決まったことが増えたら SPEC.md を更新する
- `TODO.md` は次にやること。済んだ項目は「完了済み」へ移す

## 単語データ（`js/words.js`）を触るとき
- `id` は通し番号で**永久欠番**。一度使った番号は二度と使わない。レアリティを変えても `id` は変えない
- 例文（`ex`）には、**その英単語の語幹がそのまま見える形**で入れる。カードは `hl()` が `/\b(en\w*)/i` で太字にするので、
  語幹が変わる形は太字にならない（`eat` → `eats` は可、`ate` は不可。`go` → `goes` は可、`went` は不可）
- **その単語が絵の主役になるように書く**。例文はそのまま画像のプロンプトになるので、主語が別のものに移ると
  カードの単語が絵の脇役になる（○ 鏡に幽霊が映る／× ゴブリンが鍵を落とす）
- **打ち消し・禁止・呼びかけの例文にしない**（"Do not open ..." / "Help me!" / "Listen!"）。
  絵は「しない」も「叫び声」も描けないので、その動作が絵に出ず、何の絵か分からなくなる
- **願望・誘い・抽象も避ける**（"I want to buy" / "Let's make" / "loves his home" / "a good day"）。買う・作る・愛する・良い、が絵のどこにも出ない
- **レアリティの画像を作り始める前に、そのレアリティの例文をまとめて見直す。** COMMON は1枚ずつ見つけて直したので、`open` / `close` / `jump` などで生成が何度もむだになった。UNCOMMON は生成の前に100語を見直し、64語を書き換えた
- **同じ場面が続かないようにする**（例：`usually` / `continue` / `always` が3つとも「スケルトンがよみがえる」だった）
- **中世の世界に無いもの**（自転車・駅・病院など）は、その世界にあるものとして書く（木の自転車、馬車の宿場、石造りの施療院）
- **`carry` → `carries` のように語幹が変わる形は太字にならない**（`carr` までしか一致しない）。主語を複数にして原形を使う
- **場所を例文に入れておくと安全**（"at the market" / "by the lake"）。入れないと `PLACE` の舞台が足され、湖の舟づくりに「峠」、剣を買う場面に「廃塔」のような食い違いが起きる
- **門（gate）のような構造が込み入ったものは、生成AIが形を崩しやすい**。動作が主役の語では、形の変化が一目で分かるものを使う。
  「ドアを閉める」も、手がドアの端をつかむだけの絵になり、木の棒を持っているように見えた。閉じかけの本のように、閉じる前と後の違いが絵に出るものがよい
- 例文には**敵役**（ゴブリン〜大悪魔。SPEC.md §6 の表）を入れる。目標は全体の約4割。
  ただし「その単語が主役」が優先で、**風景・自然・道具のカードは敵役なしのままでよい**。全部に入れる必要はない
- **画像がある単語（`card_art.js` の `CARD_IMG_NAMES`）の例文は変えない**。絵と食い違うため
- **蜘蛛を例文に出さない**（ユーザーが苦手。「今後、蜘蛛が出てくるプロンプトはやめて欲しい」）。
  `check_words.py` の `BANNED` が止める。画像がすでにある `book` / `big` / `room` は、作り直すときに例文も変える

## 変更したら必ず実行する
```bash
python3 tools/check_words.py        # id重複、例文に単語が入っているか、訳の空、敵役の割合
python3 tools/gen_card_prompts.py   # 例文を直したら、プロンプトを作り直す
python3 tools/gen_next_batch.py     # 次に作る画像の一覧を更新する
python3 tools/bake_assets.py        # 画像を足した・差し替えたら。枠とアイコンの透過、図鑑用の縮小版、画像の一覧
python3 tools/check_assets.py       # ASSET_V のずれ、カード画像の登録漏れ、画像の一覧の作り直し忘れ
```

## 画像生成のプロンプトを人に渡すとき（重要）
**必ず `tools/prompt_for.py` を通す。例文を記憶で書かない。**
```bash
python3 tools/prompt_for.py run happy friend   # 単語名で
python3 tools/prompt_for.py --next 5 COMMON    # まだ画像が無い語を、図鑑の並び順で
```
記憶で書いて2回やらかしている。`run` は "Run to the gate before it closes." と書いたが本当は
"The goblins run away from the village."、`sad` はそもそも words.js に無い単語だった。
どちらも絵が1枚むだになった。無い単語を渡すと `prompt_for.py` が止まる。
`card_image_prompts.md` と `card_image_next.md` は**自動生成**なので、直接編集しない。
`card_image_next.md` は `prompt_for.py --next` と同じプロンプトを同じ順番で並べる（先頭5件＝次に渡す5件）。
ユーザーが画像生成AIのコネクタで GitHub から直接読ませることがあるので、ここだけ別のプロンプトにしない。
ユーザーは Grok に GitHub の `card_image_next.md` を読ませて生成し、絵をここに貼る（Grok からリポジトリへの保存は、
画像が文字（base64）で書かれて途中で切れ、使えなかった）。main に反映されるのは PR のマージ後なので、
ファイルの先頭に「次に作る5件」の単語名を出し、Grok が最新を読んだかをユーザーが確かめられるようにしている。
**渡すときは「例文（日本語）」の行を省かない。** ユーザーは日本語の例文を読んで、届いた絵が例文どおりかを判断する
（英語の例文とプロンプトだけを渡したら、日本語で教えてほしいと言われた）。

## 画面を組み立てるときの注意
- **入力欄（`<input>`）を含む部分を、入力のたびに作り直さない。**
  iOS の日本語入力は「変換中」の状態を持ち、確定前にも `input` イベントが飛ぶ。
  そこで入力欄を作り直すと、新しい欄に `value` が入ったうえで IME が未確定の文字を入れ直すため、
  **「り」と打つと「りり」になる**。`focus()` や `setSelectionRange()` も変換を壊す。
  → 変わる部分だけを差し替える（図鑑の検索は `cardList()` が一覧・件数・クリアボタンだけを更新し、
    `#cd-q` には触らない）
- 検索欄に `inputmode="latin"` を付けると、スマホで日本語が打てなくなる
- この種の不具合は**デスクトップの Chromium では再現しない**（変換状態を持つ IME がないため）。
  入力欄をいじったら、要素が打鍵をまたいで同一かを確かめる（要素に目印を付けて、打った後も残っているか）
- **下のタブは `position:fixed`。`sticky` に戻さない。**
  `sticky` + `backdrop-filter` は iOS Safari で、`position:fixed` の重なり（カード詳細の `.sheet` など）を
  開いて閉じると合成レイヤーが作り直され、止まる位置がずれてバーが画面の下に隠れる（ラベルが切れる）。
  タブを流れから外しているので、`main` の `padding-bottom` で場所を空けている（片方だけ変えない）
- **カードの単語は、実際に測って大きさを決める**（`plateFont`）。単語は2〜14文字と幅が違い、
  文字数だけでは幅が決まらない（`accommodate` 11文字 の方が `responsibility` 14文字 より広い）。
  固定の大きさにすると長い単語が枠からはみ出す
- **小さく出るところに、描き込みの多いイラストアイコンを置かない。**
  `icon_gacha` は129px向けなので、22pxのボタンに置くと塊にしか見えない
- 高さは `100vh` ではなく **`100dvh`** を使う。iOS の `100vh` はアドレスバーを除いた「大きい方」の
  高さで、実際の表示領域と食い違う
- **画面下や高さまわりの不具合も、デスクトップでは再現しないことが多い**（アドレスバーが伸縮しないため）。
  直したら iPhone の実機で確かめてもらう

## 動作確認
ブラウザで実際に動かして確かめる（`file://` では枠画像の処理が効かないので、必ずHTTPで開く）。
```bash
python3 -m http.server 8765    # → http://localhost:8765/index.html
```
Chromium は `/opt/pw-browsers/chromium` にある（Playwright から `executablePath` で指定する）。

## ファイルを差し替えたら、版を上げる（重要）
- **CSS・JS を変えたら** `index.html` の `ASSET_V` を書き換える。**これを変えないと、一度読み込んだ端末は古いファイルを
  使い続ける**（JSを直しても直らない、という形で出る）。日付（`20260922` など）にしておけばよい
- **`ASSET_V` は、文字列として大きくなる順に付ける**（`20260924` → `20260924b` → `20260924c` → `20260925`）。
  アプリは「サイトの `ASSET_V` の方が大きいときだけ」更新ボタンを出すため。小さくすると更新が案内されない
- **`ASSET_V` はホームの下に「ver 〜」として出る**。ユーザーはこれでマージが反映されたかを確かめる。
  PR の説明には「マージ後、ホームの版が `〜` になっていれば反映済み」と書く
- **画像を足した・変えたら** `python3 tools/bake_assets.py`。画像の版は `ASSET_V` ではなく、
  `js/ui_manifest.js` に書かれたファイルごとのハッシュ。変わった画像だけが再ダウンロードされる
  （以前は `ASSET_V` を上げるたびに全画像 25MB を再ダウンロードしていた。「1日空けて開くと遅い」の原因）。
  一覧自体が変わるので、`ASSET_V` も上げる（`ui_manifest.js` の URL に付いているため）

```html
<script>const ASSET_V="20260922";</script>   <!-- index.html -->
```
**`index.html` の `?v=` は直接書いてある**（HTMLからは JS の定数を使えないため）ので、
`ASSET_V` と一緒に置換する。片方だけ変えると版がずれる → `tools/check_assets.py` が検査する。
- `ASSET_V` が付いている先：`index.html` の CSS / JS / favicon。一覧が無いときの予備の読み込み（`ui_images.js` / `card_art.js`）
- 新しく画像を読み込む処理を足すときは、`UI` / `UI_ICO` / `UI_FRAME` / `CARD_IMG` / `CARD_THUMB` を通す（一覧の URL が入っている）

## 起動と読み込み（重要）
- **起動時に画像を探したり、その場で加工したりしない。** 以前は、ファイルがあるかを確かめるために `assets/ui/` の
  画像を全部ダウンロードし（8.8MB）、枠とアイコンをブラウザで透過していた（枠がそろうまで13秒）。
  その間に2.2秒の待ちが切れると、**コードで描いた夜の城（旧背景）**を出してから本物に差し替えていた。
  いまは `tools/bake_assets.py` が前もって済ませ、`js/ui_manifest.js` に一覧を書く
- **図鑑の小さなカードには縮小版（`CARD_THUMB`）を使い、`loading="lazy"` にする。** 500枚を一度に並べるため。
  大きな絵（1200x800）はカード詳細だけで使う
- 直したら、キャッシュ無し・回線と CPU を遅くして測る（`Network.emulateNetworkConditions` と
  `Emulation.setCPUThrottlingRate`）。速い回線・キャッシュありでは差が出ない
- **起動画面はタップするまで閉じない**（「TAP TO START」と表示しているため）
- **マージしても、端末がすぐ新しい版になるとは限らない。** GitHub Pages は `index.html` を最大10分キャッシュさせ、
  iPhone の Safari はアプリに戻ってきてもページを読み直さない。そこで、起動3秒後とアプリに戻ってきたときに
  サイトの `index.html` をキャッシュを通さずに取りに行き、新しい `ASSET_V` があればホームに更新ボタンを出す
  （`checkUpdate`）。押すと `?v=新しい版` を付けて開き直す（URL が変わるので、途中のキャッシュを通らない）

## 画像
- **画像の生成は Claude ではできない**。プロンプトを用意し、生成は ChatGPT / Grok などに渡す
- カード画像のプロンプトの書き方は `tools/gen_card_prompts.py` の冒頭に理由つきで書いてある。
  **「簡素に」「余白を取って」と書かない**（生成AIによっては淡い水彩＋余白だらけになり、紺の枠に負ける）
- **「この単語が主役」は英語で言い切る**（`SUBJECT`）。例文を渡すだけでは、文中の別の名詞が主役になる
  （`sun` の "The sun rises over the castle." で、城が画面を占めて太陽が隅の点になった）
  目に見えない名詞（future / problem / strategy / revenue …。RARE から先はほとんどがそう）には、
  「見えないものなら、例文の人や物で表して大きく描く」と続けている
- **生きものには目と顔を描いてもらう**（`CREATURE`）。目が無いスライムは塊に見える
  ただし「顔が見える向き」までは書かない。**後ろ姿の絵もあってよい**（ユーザー判断）。
  **服を着ていない敵役（トロル・オーガ）には、`FOE_LOOK` に服を書く**（`cross` のトロルが裸の後ろ姿で、お尻が大きく描かれた。子どもも使うアプリなので出せない）
- **画風は締め（`TAIL`）ではなく `STYLE` にレアリティごとに書く。** 締めに一言置いただけでは
  生成AIの既定の絵柄に押し負ける（COMMON が 3DCG のようなつやつやした絵になった）。
  COMMON・UNCOMMON には `AVOID`（写実・3DCG・照り返しを外す）も付ける
- **RARE 以上には `ROLE`（役割の分かる服装）を付ける。** 画風に「金・宝石」が入っているため、
  例文に出てくる人まで王侯貴族の装いになる（`significant` で学者4人が宝石だらけの貴族に見えた）
- **敵役の見た目は `FOE_LOOK` に書いて、カードをまたいで揃える。** 書かないと、ゴブリンが絵ごとに
  別の生きものになる（`dog` は痩せて険しい緑の男、`go` は膝丈の丸い小鬼）。
  **ゴブリンの基準は `dog`**（ユーザー指定。`friend` / `go` / `run` / `house` も同じ姿に作り直し済みで、全部そろっている）。
  「愛嬌があるか怖いか」はレアリティでは決まらない（同じ COMMON でもスライムは可愛く、ゴブリンは怖い）ので、
  そこも `FOE_LOOK` に書く。
  `ENEMIES` に敵役を足したら `FOE_LOOK` にも見た目を書く（`assert` で漏れを止めている）。
  例文で「敵そのもの」でない語（`cursed swamp` の cursed など）は `FOE_SKIP` に入れる。
  形容詞としても使う語は `check_words.py` の `NOT_FOE_AFTER` に、後ろに続く名詞を書く
  （`giant spider`（大蜘蛛）の giant を巨人と数え、大蜘蛛の絵に巨人の説明が入っていた。例文の giant は5つとも形容詞だった）。
  **敵役でない生きものは `EXTRA_LOOK` に書く**（竜など）。`ENEMIES` に入れると
  「敵役の割合」の数字が狂う（竜は12語に出るが、"a friendly dragon" のように味方側でも出る）。
  例文が色を言う語（"A red dragon"）があるものは、見た目の側を「例文が別の色を言っていなければ」と譲らせる
- **`WORLD` は世界観だけ書く。持ち物を並べない。** 以前 "stone castles, cobbled roads, cloaks,
  swords and lanterns" と書いていたら、生成AIが律儀にその5つを毎回描き、**どのカードにも同じ城と同じ街灯**が立った。
  いまは「ヨーロッパ中世のファンタジー。和風でも中華風でも現代でもない。例文と舞台が求めるものだけを描き、
  城・街灯・道標は求められない限り足さない」と書いている
  （書かないと `drink` が日本の茶の間、`significant` の学者が中華風の道士になる）
- **舞台は `PLACE` で単語ごとに散らす**（15種類：村の通り・市場・小屋の中・森の道・港・農場・橋・回廊・
  地下倉・酒場・麦畑・峠・廃塔・書斎・鍛冶場）。ただし押しつけない:
  例文が場所を言っている語（`PLACE_CUE`）と、その語自体が場所の語（`PLACE_SELF`）には足さない。
  `PLACE_CUE` は前置詞＋冠詞の形を見ている。前置詞が抜けていると舞台が入ってしまう
  （`onto` が無くて "jumps onto the round table" に「村の通り」が入り、通りの真ん中にテーブルがある絵になった）。
  さらに文中で「場面に合うなら」と条件を付ける（屋内の舞台が屋外の場面に当たることがあるため）
- **単語そのものが、中世ヨーロッパでない形で描かれる語は `ITEM_LOOK` に形を書く**（キーは単語）。
  `umbrella` が骨の細い和傘になり、少女の顔立ちも和風に見えた。`WORLD` の「和風でない」だけでは足りなかった
- **`FACES`（複数人は顔を描き分ける）も入れる。** `significant` の学者3人が同じ顔だった
- **構図は `SHOT` で単語ごとに散らす。** 全部に「主役を中央に大きく」と書いていたら、
  どれも「真ん中に一つ、同じ距離・同じ高さ」の同じ絵面になった。`id` で8種類に振り分けるので、
  図鑑で隣り合うカードは必ず別の構図になる。
  変えてよいのは**カメラの位置・距離・横の置き場所・前景まで**。
  「その単語が主役」と「大事なものは中央の帯に収める」は崩さない（`SUBJECT` と `TAIL` が受け持つ）。
  **明るさ・時刻・天気は入れない**（例文と食い違う。"The sun rises over the castle." に「夜」とは書けない）
- **届いた絵が 3:2 でないときは、引き伸ばさずに中央で 3:2 に切り出す**（生成AIが 16:9 で返すことがある。`speak` は 1792x1008 だった）。切ったあと、主役が枠に残っているかを図鑑で確かめる
- アイコンは**マゼンタ背景**で受け取る（生成AIは透過PNGを安定して出せない）。
  `ui_images.js` が透過して余白を詰めるので、大きさがそろう。
  透過済みの画像はそのまま使われる（紫の発光を削らないため）
- 置き場と命名は SPEC.md §12 と `assets/ui/README.md` のとおり
